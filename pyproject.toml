[project]
name = "funding-sentinel"
version = "0.1.0"
description = "Multi-exchange crypto funding rate and volume anomaly monitor."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
funding-sentinel = "funding_sentinel.main:run"

[tool.pytest.ini_options]
testpaths = ["tests"]
